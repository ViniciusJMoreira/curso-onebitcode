const x = Number(prompt("Informe o primeiro nome: "))
const y = Number(prompt("Informe o segundo nome: "))

const addition = x + y
const subtraction = x - y
const division = x / y
const multiplication = x * y

alert (
  "Soma: " + addition +
  "\nSubtração: " + subtraction +
  "\nDivisão: " + division +
  "\nMultiplicação: " + multiplication
)