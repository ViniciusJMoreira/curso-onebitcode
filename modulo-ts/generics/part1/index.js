"use strict";
function first(array) {
    return array[0];
}
function last(array) {
    return array[array.length - 1];
}
const array = ['', '', ''];
const firstArray = first(array);
const lastArray = last(array);
//
function example(arg) {
    return arg;
}
const string = example('string');
const number = example(2);
